spring:
  config:
    import: optional:configserver:http://localhost:8888/
  cloud:
    config:
      token: roottoken
  jpa:
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: always